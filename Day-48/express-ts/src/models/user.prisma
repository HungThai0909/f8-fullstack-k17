model User {
    id        Int       @id @default(autoincrement())
    email     String    @unique
    name      String
    status    Boolean
    phone     Phone?
    posts     Post[]
    products Product[]
    createdAt DateTime? @db.Timestamp()
    updatedAt DateTime? @db.Timestamp()

    @@map("users")
}
